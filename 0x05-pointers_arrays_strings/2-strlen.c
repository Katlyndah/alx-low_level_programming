#include "main.h"
#include <string.h>

/**
 * _strlen -> function that returns the length of a string.
 * @s: string to count
 * Return: length of the string
 */
int _strlen(char *s)
{
	int l = 0;

	while (*s != '\0')
	{
		s++;
		l++;
	}

	return (1);
}
